# This is the Codeception Test Suite Configuration
# To use it rename this file to acceptance.suite.yml (it will be ignored by git)
# To run the test modify the following parameters according to your localhost details:
#  - url
#  - folder
#  - db_user and db_pass
# suite for acceptance tests.
# perform tests in browser using the WebDriver or PhpBrowser.
# If you need both WebDriver and PHPBrowser tests - create a separate suite.

class_name: AcceptanceTester
modules:
    enabled:
        - WebDriver
        - AcceptanceHelper
        - Filesystem
    config:
        WebDriver:
            url: 'http://vowel07-php7.local/JTSite/administrator'  # the url that points to the joomla installation at /tests/system/joomla-cms
            browser: 'chrome'
            window_size: 2048x1024
            capabilities:
              unexpectedAlertBehaviour: 'accept'
        AcceptanceHelper:                          # see: tests/_support/acceptancehelper.php
            Username: 'pranoti'                          # UserName for the Administrator
            Password: 'patilp24'                      # Password for the Administrator
            SiteUsername: 'pranoti'                          # UserName for the Administrator
            SitePassword: 'patilp24'                      # Password for the Administrator
            Database User: 'root'                  # MySQL Server user ID, usually root
            Database Password: 'root'                  # MySQL Server password, usually empty or root
            Database Name: 'JTSite'                   # DB Name, at the Server
            Database Type: 'mysqli'                # type in lowercase one of the options: MySQL\MySQLi\PDO
            Database Prefix: 'nsi6m_'                # DB Prefix for tables
            Install Sample Data: 'no'              # Do you want to Download the Sample Data Along with Joomla Installation, then keep it Yes
            Sample Data: 'Default English (GB) Sample Data'    # Default Sample Data
            Admin email: 'admin@mydomain.com'      # email Id of the Admin
            Language: 'English (United Kingdom)'   # Language in which you want the Application to be Installed
            Joomla folder: '/home/Document/git/joomla-cms/tests/codeception/testingsite/' # Path to Joomla installation where we execute the tests
